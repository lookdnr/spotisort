# Runtime dependencies for spotisort.
#
# pyproject.toml is the source of truth for dependency metadata; this file is a
# convenience mirror for `pip install -r requirements.txt` workflows.
#
# For development tooling (pytest, mypy, ruff) install the optional extras instead:
#     pip install -e ".[dev]"

spotipy>=2.24
python-dotenv>=1.0
